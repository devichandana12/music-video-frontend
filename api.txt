new project using Api CRUD operation
----------------------------------

    REACT App using Json server
    ---------------------------

        objective

        --> create a web applications using REACT js with the below problems statement

            --> problem statement

            --> create an applications where the followinf functionalities are present 

                1. user can create, delete edit card
                2. the card must contain name and a video/mp3 link 
                3. the user has the flxibility to create bucket to categories and card according to his/her choice
                4. user can move an card to different bucket
                5. on clicking on the card a model with an iframe  should open which start playing the video
                6. user can create delete a single card or multiple  cards  under a single bucket at once 
                7. there should be a sepereate tab called history which list the mp3/mp4 links that you have played 
                
                    i.e the card name the link and the item it was played 

                        tech stack
                        -----------

                            1. front-end : react 
                            2. back-end :  create and use json server
                            3. any additional  stacks/tools as per your performance



redirecting from one page to another

1. install reactrouter dom -->npm i react-router-dom
2. entry point--> app.js nnta selector nna browserrouter ll warp 








10-11-2023


create a watch history


            1.create a watch history tab in home page
            2.create a component for watch history
            3.show that data in table format
            4.create a watch key in db.json
            5.get watch history from json file to watch history component